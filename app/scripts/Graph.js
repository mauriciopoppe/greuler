'use strict';

import extend from 'extends';
import util from './utils';

class Graph {
  constructor(owner, nodes, edges) {
    this.owner = owner;
    this.nodes = nodes;
    this.edges = edges;
  }

  adjacencyList() {

  }

  getNode() {

  }

  getEdge(source, target) {
  }
}
